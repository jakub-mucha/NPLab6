#include <stdio.h>
#include "function.h"
int main()
{
	double end,val = 60;
	int var = 123, endpoint,twenty;
	end = sinus(val);
	printf(end,%d);
	printf("/n");
	endpoint = randomize(var)
	printf(endpoint, %i);
	printf("/n");
	twenty = multiex(var,endpoint);
	printf(twenty, %i);
	printf("/n");
	return 0;
}
