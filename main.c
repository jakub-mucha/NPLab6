#include <stdio.h>
#include "function.h"
int main()
{
	double end,val = 60;
	int var = 123, endpoint;
	end = sinus(val);
	printf(end,%d);
	endpoint = randomize(var)
	printf(endpoint, %i);
	return 0;
}
